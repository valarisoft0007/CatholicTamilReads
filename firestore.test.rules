rules_version = '2';
// Open rules for local testing with Firebase emulator only.
// Never deploy this file to production.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
